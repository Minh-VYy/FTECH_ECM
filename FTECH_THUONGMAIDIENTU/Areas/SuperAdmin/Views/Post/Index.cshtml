@using FTECH_THUONGMAIDIENTU.Models.Admin
@{
    ViewBag.Title = "Duyệt Bài Viết";
    Layout = "~/Areas/SuperAdmin/Views/Shared/_SuperAdminLayout.cshtml";
    var currentStatus = (ViewBag.CurrentStatus ?? "all").ToString();
    int total    = ViewBag.TotalCount    ?? 0;
    int draft    = ViewBag.DraftCount    ?? 0;
    int pending  = ViewBag.PendingCount  ?? 0;
    int approved = ViewBag.ApprovedCount ?? 0;
    int rejected = ViewBag.RejectedCount ?? 0;
    var posts    = ViewBag.Posts as List<PostViewModel> ?? new List<PostViewModel>();
}

<div class="topbar">
    <div class="topbar-title">Duyệt Bài Viết</div>
    <div class="topbar-search">
        <input type="text" id="searchInput" placeholder="Tìm tiêu đề, tác giả, danh mục...">
        <button onclick="filterTable()">🔍</button>
    </div>
    <div class="topbar-actions">
        <button class="btn-ghost" onclick="exportCSV()">Xuất CSV</button>
        <button class="btn-primary" id="pendingQuickBtn" onclick="filterByStatus('pending')">
            Bài chờ duyệt (@pending)
        </button>
    </div>
</div>

<div class="content">
    <!-- KPI Stats -->
    <div class="stat-row">
        <div class="stat-card sc-all">
            <div class="stat-icon" style="font-size:22px">📋</div>
            <div>
                <div class="stat-label">Tổng bài viết</div>
                <div class="stat-val sv-all">@total</div>
                <div class="stat-sub">Toàn bộ nội dung trong hệ thống</div>
            </div>
        </div>
        <div class="stat-card sc-draft">
            <div class="stat-icon" style="font-size:22px">📝</div>
            <div>
                <div class="stat-label">Bản nháp</div>
                <div class="stat-val sv-draft">@draft</div>
                <div class="stat-sub">Chờ gửi lên duyệt</div>
            </div>
        </div>
        <div class="stat-card sc-pending">
            <div class="stat-icon" style="font-size:22px">⏳</div>
            <div>
                <div class="stat-label">Chờ duyệt</div>
                <div class="stat-val sv-pending">@pending</div>
                <div class="stat-sub">Ưu tiên kiểm tra hôm nay</div>
            </div>
        </div>
        <div class="stat-card sc-approved">
            <div class="stat-icon" style="font-size:22px">✅</div>
            <div>
                <div class="stat-label">Đã duyệt</div>
                <div class="stat-val sv-approved">@approved</div>
                <div class="stat-sub">Đang hiển thị công khai</div>
            </div>
        </div>
        <div class="stat-card sc-rejected">
            <div class="stat-icon" style="font-size:22px">❌</div>
            <div>
                <div class="stat-label">Từ chối</div>
                <div class="stat-val sv-rejected">@rejected</div>
                <div class="stat-sub">Đã gửi yêu cầu chỉnh sửa</div>
            </div>
        </div>
    </div>

    <!-- Filter chips -->
    <div class="filter-wrap">
        <button class="fchip @(currentStatus == "all"      ? "active" : "")"               onclick="filterByStatus('all')">Tất cả (@total)</button>
        <button class="fchip fc-draft    @(currentStatus == "draft"    ? "active" : "")"   onclick="filterByStatus('draft')">Nháp (@draft)</button>
        <button class="fchip fc-pending  @(currentStatus == "pending"  ? "active" : "")"   onclick="filterByStatus('pending')">Chờ duyệt (@pending)</button>
        <button class="fchip fc-approved @(currentStatus == "approved" ? "active" : "")"   onclick="filterByStatus('approved')">Đã duyệt (@approved)</button>
        <button class="fchip fc-rejected @(currentStatus == "rejected" ? "active" : "")"   onclick="filterByStatus('rejected')">Từ chối (@rejected)</button>
        <select class="fsel" style="margin-left:auto" onchange="filterByCategory(this.value)">
            <option value="">Tất cả danh mục</option>
        </select>
        <select class="fsel" onchange="sortPosts(this.value)">
            <option value="newest">Mới cập nhật</option>
            <option value="oldest">Cũ nhất</option>
        </select>
    </div>

    <!-- Bulk action bar -->
    <div class="bulk-bar" id="bulkBar">
        <span class="bulk-info" id="bulkInfo">0 bài được chọn</span>
        <button class="btn-ghost" onclick="bulkApprove()">✅ Duyệt tất cả</button>
        <button class="btn-ghost" style="color:#ef4444;border-color:rgba(239,68,68,.3)" onclick="bulkReject()">❌ Từ chối tất cả</button>
    </div>

    <!-- Table -->
    <div class="table-wrap">
        <div class="t-head">
            <div><input type="checkbox" id="selectAll" onchange="toggleSelectAll(this)"></div>
            <div class="th">Bài viết</div>
            <div class="th">Tác giả</div>
            <div class="th">Danh mục</div>
            <div class="th">Trạng thái</div>
            <div class="th">Lượt xem</div>
            <div class="th">Click AFF</div>
            <div class="th">Cập nhật</div>
            <div class="th">Thao tác</div>
        </div>

        @if (!posts.Any())
        {
            <div style="padding:40px;text-align:center;color:#8a93b8;font-size:14px">
                📭 Không có bài viết nào trong trạng thái này
            </div>
        }

        @foreach (var post in posts)
        {
            <div class="t-row" data-id="@post.Id" data-status="@post.Status" data-category="@post.Category">
                <div><input type="checkbox" class="row-cb" value="@post.Id" onchange="updateBulkBar()"></div>
                <div class="post-cell">
                    <div class="post-thumb">
                        <img src="@post.ThumbSafe" alt="thumb" onerror="this.style.display='none'">
                    </div>
                    <div>
                        <div class="post-title">@post.Title</div>
                        <div class="post-meta">@post.Author · @post.AffLinkCount affiliate link hoạt động</div>
                    </div>
                </div>
                <div style="font-size:13px;color:#303a75;font-weight:600">@post.Author</div>
                <div><span class="cat-tag">@post.Category</span></div>
                <div><span class="sp @post.StatusCss">@post.StatusLabel</span></div>
                <div class="metric">@post.ViewCount.ToString("N0")</div>
                <div class="metric @(post.ClickCount > 0 ? "metric-g" : "metric-m")">@post.ClickCount.ToString("N0")</div>
                <div style="font-size:12px;color:#8a93b8">@post.CreatedAt.ToString("dd/MM/yyyy")</div>
                <div class="row-acts">
                    <button class="act act-preview" title="Preview"
                            onclick="openPreview(@post.Id)">👁</button>
                    <button class="act" title="Chỉnh sửa"
                            onclick="openEdit(@post.Id, '@HttpUtility.JavaScriptStringEncode(post.Title)', '@HttpUtility.JavaScriptStringEncode(post.Status)')">✏</button>
                    @if (post.Status == "Chờ duyệt" || post.Status == "Nháp")
                    {
                        <button class="act act-approve" title="Duyệt"
                                onclick="approvePost(@post.Id)">✓</button>
                        <button class="act act-reject" title="Từ chối"
                                onclick="openReject(@post.Id, '@HttpUtility.JavaScriptStringEncode(post.Title)')">✕</button>
                    }
                    @if (post.Status == "Đã xuất bản")
                    {
                        <button class="act act-reject" title="Thu hồi"
                                onclick="openReject(@post.Id, '@HttpUtility.JavaScriptStringEncode(post.Title)')">↩</button>
                    }
                </div>
            </div>
        }

        <div class="pagination">
            <div class="pag-info">Hiển thị @posts.Count / @total bài viết</div>
            <div class="pag-btns">
                <button class="pag-btn active">1</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal: Preview -->
<div class="modal-overlay" id="previewModal">
    <div class="preview-modal">
        <div class="pv-head">
            <div class="pv-title">Preview kiểm duyệt bài viết</div>
            <button class="pv-close" onclick="closeModal('previewModal')">✕</button>
        </div>
        <div class="pv-body" id="previewBody">
            <div style="text-align:center;padding:40px;color:#8a93b8">Đang tải...</div>
        </div>
        <div class="pv-foot">
            <button class="modal-btn-cancel" onclick="closeModal('previewModal')">Đóng</button>
            <button class="modal-btn-confirm" style="background:linear-gradient(135deg,#ef4444,#f97316)"
                    id="previewRejectBtn" onclick="openRejectFromPreview()">✕ Từ chối</button>
            <button class="modal-btn-confirm" style="background:linear-gradient(135deg,#1bcf8a,#10b981)"
                    id="previewApproveBtn" onclick="approveFromPreview()">✓ Duyệt bài</button>
        </div>
    </div>
</div>

<!-- Modal: Từ chối -->
<div class="modal-overlay" id="rejectModal">
    <div class="modal">
        <div class="modal-title">🚫 Từ chối bài viết</div>
        <div class="modal-sub">Vui lòng nhập lý do để tác giả biết cần chỉnh sửa trước khi gửi duyệt lại.</div>
        <div class="modal-post-ref">📄 <span id="rejectPostTitle">Tiêu đề bài viết</span></div>
        <input type="hidden" id="rejectPostId">
        <label class="fl">Lý do từ chối *</label>
        <textarea class="modal-ta" id="rejectReason" placeholder="Ví dụ: nội dung chưa đầy đủ, thiếu affiliate link..."></textarea>
        <div style="margin-top:14px">
            <div style="font-size:11px;font-weight:700;letter-spacing:1px;text-transform:uppercase;color:#8a93b8;margin-bottom:10px">Mức độ</div>
            <label style="margin-right:16px;font-size:13px"><input type="radio" name="rejectLevel" value="minor"> Sửa nhỏ</label>
            <label style="margin-right:16px;font-size:13px"><input type="radio" name="rejectLevel" value="major" checked> Sửa lại nhiều</label>
            <label style="font-size:13px"><input type="radio" name="rejectLevel" value="notaccept"> Không đăng</label>
        </div>
        <div class="modal-actions">
            <button class="modal-btn-cancel" onclick="closeModal('rejectModal')">Hủy</button>
            <button class="modal-btn-confirm" onclick="submitReject()">Gửi từ chối →</button>
        </div>
    </div>
</div>

<!-- Modal: Cập nhật -->
<div class="modal-overlay" id="editModal">
    <div class="modal" style="width:520px">
        <div class="modal-title">✏️ Cập nhật bài viết</div>
        <div class="modal-sub">Cập nhật tiêu đề và trạng thái bài viết ngay từ màn hình quản lý.</div>
        <div class="modal-post-ref">📄 <span id="editPostRef">Tiêu đề</span></div>
        <input type="hidden" id="editPostId">
        <div class="form-grid" style="margin-bottom:12px">
            <div>
                <label class="fl">Tiêu đề bài viết</label>
                <input class="input-field" id="editTitle" type="text" placeholder="Nhập tiêu đề">
            </div>
            <div>
                <label class="fl">Trạng thái</label>
                <select class="input-field select-field" id="editStatus">
                    <option value="Nháp">Bản nháp</option>
                    <option value="Chờ duyệt">Chờ duyệt</option>
                    <option value="Đã xuất bản">Đã duyệt</option>
                    <option value="Từ chối">Từ chối</option>
                </select>
            </div>
        </div>
        <label class="fl">Ghi chú cập nhật</label>
        <textarea class="modal-ta" id="editNote" placeholder="Ví dụ: cập nhật mô tả ngắn, chỉnh affiliate link..."></textarea>
        <div class="modal-actions">
            <button class="modal-btn-cancel" onclick="closeModal('editModal')">Đóng</button>
            <button class="modal-btn-confirm" style="background:linear-gradient(135deg,#4a7dff,#8a5bff)"
                    onclick="submitEdit()">Lưu cập nhật</button>
        </div>
    </div>
</div>

<div id="toast" style="position:fixed;bottom:28px;right:28px;background:#1bcf8a;color:#fff;padding:14px 22px;border-radius:12px;font-size:13px;font-weight:600;box-shadow:0 8px 32px rgba(42,47,95,.25);z-index:9999;display:none;max-width:340px"></div>

@section scripts {
    <script src="~/Scripts/js/common/auth.js"></script>
    <script>
        var APPROVE_URL = '@Url.Action("Approve",    "Post", new { area = "SuperAdmin" })';
        var REJECT_URL  = '@Url.Action("Reject",     "Post", new { area = "SuperAdmin" })';
        var UPDATE_URL  = '@Url.Action("UpdatePost", "Post", new { area = "SuperAdmin" })';
        var DETAIL_URL  = '@Url.Action("GetDetail",  "Post", new { area = "SuperAdmin" })';
        var PAGE_URL    = '@Url.Action("Index",      "Post", new { area = "SuperAdmin" })';
        var currentPreviewId = null;

        function showToast(msg, ok) {
            var t = document.getElementById('toast');
            t.textContent = msg;
            t.style.background = ok ? '#1bcf8a' : '#ef4444';
            t.style.display = 'block';
            setTimeout(function() { t.style.display = 'none'; }, 3200);
        }
        function closeModal(id) { document.getElementById(id).classList.remove('open'); }

        function filterByStatus(st) { window.location.href = PAGE_URL + '?status=' + st; }

        function filterTable() {
            var q = document.getElementById('searchInput').value.toLowerCase();
            document.querySelectorAll('.t-row').forEach(function(r) {
                r.style.display = r.innerText.toLowerCase().includes(q) ? '' : 'none';
            });
        }
        function filterByCategory(cat) {
            document.querySelectorAll('.t-row').forEach(function(r) {
                r.style.display = !cat || r.dataset.category === cat ? '' : 'none';
            });
        }
        function sortPosts(v) { /* server-side sort on reload */ }

        function toggleSelectAll(cb) {
            document.querySelectorAll('.row-cb').forEach(function(c) { c.checked = cb.checked; });
            updateBulkBar();
        }
        function updateBulkBar() {
            var n = document.querySelectorAll('.row-cb:checked').length;
            document.getElementById('bulkBar').classList.toggle('show', n > 0);
            document.getElementById('bulkInfo').textContent = n + ' bài được chọn';
        }
        function getSelectedIds() {
            return Array.from(document.querySelectorAll('.row-cb:checked')).map(function(c) { return c.value; });
        }

        function approvePost(id) {
            if (!confirm('Duyệt bài viết này?')) return;
            postAction(APPROVE_URL, { id: id }, function(d) {
                showToast(d.message, d.success);
                if (d.success) setTimeout(function() { location.reload(); }, 1200);
            });
        }
        function approveFromPreview() { if (currentPreviewId) approvePost(currentPreviewId); }
        function bulkApprove() {
            var ids = getSelectedIds();
            if (!ids.length || !confirm('Duyệt ' + ids.length + ' bài?')) return;
            var done = 0;
            ids.forEach(function(id) {
                postAction(APPROVE_URL, { id: id }, function() {
                    done++;
                    if (done === ids.length) { showToast('Đã duyệt ' + ids.length + ' bài!', true); setTimeout(function() { location.reload(); }, 1400); }
                });
            });
        }

        function openReject(id, title) {
            currentPreviewId = id;
            document.getElementById('rejectPostId').value = id;
            document.getElementById('rejectPostTitle').textContent = title;
            document.getElementById('rejectReason').value = '';
            document.getElementById('rejectModal').classList.add('open');
        }
        function openRejectFromPreview() {
            var title = (document.querySelector('#previewBody .pv-post-title') || {}).textContent || '';
            closeModal('previewModal');
            openReject(currentPreviewId, title);
        }
        function submitReject() {
            var id     = document.getElementById('rejectPostId').value;
            var reason = document.getElementById('rejectReason').value.trim();
            if (!reason) { showToast('Vui lòng nhập lý do từ chối.', false); return; }
            postAction(REJECT_URL, { id: id, reason: reason }, function(d) {
                closeModal('rejectModal');
                showToast(d.message, d.success);
                if (d.success) setTimeout(function() { location.reload(); }, 1200);
            });
        }
        function bulkReject() {
            var ids = getSelectedIds();
            if (!ids.length) return;
            var reason = prompt('Lý do từ chối (áp dụng cho ' + ids.length + ' bài):');
            if (!reason) return;
            var done = 0;
            ids.forEach(function(id) {
                postAction(REJECT_URL, { id: id, reason: reason }, function() {
                    done++;
                    if (done === ids.length) { showToast('Đã từ chối ' + ids.length + ' bài.', true); setTimeout(function() { location.reload(); }, 1400); }
                });
            });
        }

        function openPreview(id) {
            currentPreviewId = id;
            document.getElementById('previewModal').classList.add('open');
            document.getElementById('previewBody').innerHTML = '<div style="text-align:center;padding:40px;color:#8a93b8">Đang tải...</div>';
            fetch(DETAIL_URL + '?id=' + id, { headers: { 'X-Requested-With': 'XMLHttpRequest' } })
                .then(function(r) { return r.json(); })
                .then(function(data) {
                    if (!data.success) {
                        document.getElementById('previewBody').innerHTML = '<div style="color:#ef4444;padding:20px">' + data.message + '</div>';
                        return;
                    }
                    var d = data.data;
                    var thumb = d.thumbnail ? '<div class="pv-thumb" style="background:url(\'' + d.thumbnail + '\') center/cover no-repeat;height:200px;border-radius:12px;margin-bottom:18px"></div>' : '';
                    document.getElementById('previewBody').innerHTML =
                        thumb +
                        '<span class="pv-tag">' + (d.category || 'Bài viết') + '</span>' +
                        '<div class="pv-post-title" style="font-size:20px;font-weight:700;color:#303a75;margin:10px 0">' + d.title + '</div>' +
                        '<div class="pv-meta" style="font-size:12px;color:#8a93b8;display:flex;gap:14px;margin-bottom:18px">' +
                            '<span>👤 ' + d.author + '</span>' +
                            '<span>📅 ' + d.createdAt + '</span>' +
                            '<span>👁 ' + d.viewCount + ' lượt xem</span>' +
                        '</div>' +
                        '<div style="font-size:14px;color:#5d6897;line-height:1.8">' + (d.content || '').substring(0, 800) + (d.content && d.content.length > 800 ? '...' : '') + '</div>';

                    var pend = d.status === 'Chờ duyệt' || d.status === 'Nháp';
                    document.getElementById('previewApproveBtn').style.display = pend ? '' : 'none';
                    document.getElementById('previewRejectBtn').style.display  = pend || d.status === 'Đã xuất bản' ? '' : 'none';
                })
                .catch(function() {
                    document.getElementById('previewBody').innerHTML = '<div style="color:#ef4444;padding:20px">Lỗi tải dữ liệu.</div>';
                });
        }

        function openEdit(id, title, status) {
            document.getElementById('editPostId').value = id;
            document.getElementById('editPostRef').textContent = title;
            document.getElementById('editTitle').value = title;
            document.getElementById('editStatus').value = status;
            document.getElementById('editNote').value = '';
            document.getElementById('editModal').classList.add('open');
        }
        function submitEdit() {
            var id     = document.getElementById('editPostId').value;
            var title  = document.getElementById('editTitle').value.trim();
            var status = document.getElementById('editStatus').value;
            var note   = document.getElementById('editNote').value.trim();
            if (!title) { showToast('Vui lòng nhập tiêu đề.', false); return; }
            postAction(UPDATE_URL, { id: id, title: title, statusVal: status, note: note }, function(d) {
                closeModal('editModal');
                showToast(d.message, d.success);
                if (d.success) setTimeout(function() { location.reload(); }, 1200);
            });
        }

        function exportCSV() {
            var rows = [['ID','Tiêu đề','Tác giả','Danh mục','Trạng thái','Lượt xem','Click AFF','Ngày']];
            document.querySelectorAll('.t-row').forEach(function(row) {
                rows.push([
                    row.dataset.id,
                    (row.querySelector('.post-title') || {}).textContent || '',
                    row.children[2].textContent.trim(),
                    row.children[3].textContent.trim(),
                    row.children[4].textContent.trim(),
                    row.children[5].textContent.trim(),
                    row.children[6].textContent.trim(),
                    row.children[7].textContent.trim()
                ]);
            });
            var csv  = rows.map(function(r) { return r.map(function(c) { return '"' + String(c).replace(/"/g,'""') + '"'; }).join(','); }).join('\n');
            var blob = new Blob(['\ufeff' + csv], { type: 'text/csv;charset=utf-8;' });
            var a    = document.createElement('a');
            a.href   = URL.createObjectURL(blob);
            a.download = 'bai-viet-' + new Date().toISOString().slice(0,10) + '.csv';
            a.click();
        }

        function postAction(url, params, cb) {
            var body = Object.keys(params).map(function(k) { return k + '=' + encodeURIComponent(params[k]); }).join('&');
            fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'X-Requested-With': 'XMLHttpRequest' }, body: body })
                .then(function(r) { return r.json(); }).then(cb)
                .catch(function() { showToast('Lỗi kết nối máy chủ.', false); });
        }

        document.getElementById('searchInput').addEventListener('keyup', function(e) { if (e.key === 'Enter') filterTable(); });
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') ['previewModal','rejectModal','editModal'].forEach(function(id) { closeModal(id); });
        });
    </script>
}
